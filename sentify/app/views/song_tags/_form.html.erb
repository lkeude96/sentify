<%= form_for(@song_tag) do |f| %>
  <% if @song_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_tag.errors.count, "error") %> prohibited this song_tag from being saved:</h2>

      <ul>
      <% @song_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :song_id %><br>
    <%= f.text_field :song_id %>
  </div>
  <div class="field">
    <%= f.label :tag_name %><br>
    <%= f.text_field :tag_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
